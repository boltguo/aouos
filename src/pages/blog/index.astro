---
import Layout from '../../layouts/Layout.astro';
import Nav from '../../components/home/Nav.astro';
import { ArrowRight } from '@lucide/astro';
import { getCollection } from 'astro:content';

const posts = (await getCollection('blog')).sort(
  (a, b) => new Date(b.data.date).getTime() - new Date(a.data.date).getTime()
);

const formatDate = (value: Date) =>
  value.toLocaleDateString('en-US', {
    month: 'short',
    day: '2-digit',
    year: 'numeric',
  });
---

<Layout
  title="Blog — BoltGuo"
  description="Thoughts and notes from BoltGuo on frontend engineering and AI."
>
  <div
    class="bg-background-light relative min-h-screen overflow-x-hidden text-gray-900 transition-colors duration-300"
  >
    <Nav />

    <section class="relative z-10 pt-32 pb-16 lg:pt-44">
      <div class="mx-auto max-w-4xl space-y-4 px-4 text-center sm:px-6 lg:px-8">
        <h1 class="text-4xl font-bold tracking-tight text-gray-900 lg:text-5xl">
          Thoughts &amp; Insights
        </h1>
        <p class="text-lg text-gray-600">
          Notes on building clean interfaces, collaborating with AI, and keeping
          products fast.
        </p>
      </div>
    </section>

    <section class="relative z-10 pb-20">
      <div class="mx-auto max-w-4xl space-y-10 px-4 sm:px-6 lg:px-8">
        {
          posts.map((post) => (
            <a
              class="group relative block border border-gray-100 bg-white p-8 transition-colors"
              href={`/blog/${post.id}`}
            >
              <div class="bg-primary absolute top-0 left-0 h-full w-1 origin-top scale-y-0 transform transition-transform duration-300 group-hover:scale-y-100" />
              <div class="mb-4 flex flex-col md:flex-row md:items-baseline md:justify-between">
                <h2 class="text-2xl font-bold text-gray-900 transition-colors">
                  {post.data.title}
                </h2>
                <span class="mt-2 shrink-0 font-mono text-xs text-gray-400 md:mt-0">
                  {formatDate(post.data.date)}
                </span>
              </div>
              <p class="mb-6 leading-relaxed text-gray-600">
                {post.data.description}
              </p>
              <div class="text-primary flex items-center text-sm font-medium">
                <span>Read Article</span>
                <ArrowRight class="ml-2 h-4 w-4" />
              </div>
            </a>
          ))
        }
      </div>
    </section>
  </div>
</Layout>
